# Spline Smudge — 大まかなタスクリスト

作成日：2026-09-10  
凡例：`[x]` 完了、`[ ]` 未完了または評価待ち。見積もりや締切は未設定。

初期目標は「写真1枚・スプライン1本でA/Bを比較できること」。複数線と制作版の完成は、その後の段階に分ける。

## 0. 計画と置き場所

- [x] 独立したプロジェクトフォルダ `/Users/ayato/spline-smudge` を作成する。
- [x] 合意した仕様と未確定事項をプロジェクト計画書にまとめる。
- [x] 段階ごとのタスクと、相談して判断する箇所を共有する。

計画段階を完了し、Prototype 01を実装済み。

## 1. 土台と画像読み込み

- [x] TypeScript＋WebGL2＋GLSLの最小実行環境を作る。
- [x] WebGL2の動作・画像サイズ上限を確認し、起動手順を用意する。
- [x] 画像読み込み前に、初期の色空間・階調・透過の扱いと制約を相談する。
- [x] JPEG／PNGの読み込み、向き・縦横比を保つ表示、元画像の保持を実装する。
- [x] 画面に収める表示、100％表示、拡大・移動の基本操作を作る。

到達点：異なるサイズ・比率の写真を読み込み、画像を拡大して確認できる。

## 2. 1本のスプライン編集を移植

- [x] 既存の5種類の曲線計算をp5に依存しない形で移す。
- [x] 点の追加・移動・ダブルクリック削除、選択、番号・補助表示を作る。
- [x] 基本線幅pxと点ごとの倍率0〜10倍を移す。
- [x] 取り消しと、将来の複数線を格納できるストロークデータ構造を用意する。

到達点：既存ツールと同様に、1本の線の形と太さを編集できる。

## 3. Aの表現を試作

- [x] 採取位置・方向・範囲の操作案を確認する。
- [x] 写真の複数の色を並びとして採取し、スプラインに沿って伸ばすGPU描画を作る。
- [ ] 太さの変化、折り返し、強い曲がり、交差、端点での見た目を確認する。
- [x] 点を動かしても元写真から再描画でき、取り消しで戻れることを確認する。

到達点：写真由来の色の筋が連続するストロークを編集できる。

## 4. Bの表現を試作し、A/Bを比較

- [x] 旧Bの混色ブラシを評価し、各点の採取線を補間する新Bへ変更する。
- [x] 各点の採取角度・長さの編集と、隣接点間のGPU色補間を作る。
- [x] RGB補間を独立した関数へ分離し、後から方式を変更できるようにする。
- [x] 点や幅の変更時に、元写真から1本を再計算する。
- [x] A/B切り替え時に写真・点配置を保持し、各モードの採取設定も保持する。
- [x] 長い処理を中断できるようにし、古い編集要求の処理を蓄積させない。
- [ ] 同じ写真・同じ曲線で、見た目と操作待ち時間を比較する。

判断：A/Bの見た目が意図に近いか。Bをどう残すか。必要なら画像加工方式を見直す。

## 5. 高解像度・出力・性能の検証

- [x] 長辺5000px程度の画像で、A/Bの計算と検証用出力を試す。
- [ ] 色の筋、輪郭、ぼけ、階調の段差を100％表示で確認する。
- [ ] 写真の読み込み、点編集、Bの再計算、書き出しにかかる時間を測る。
- [x] GPU資源の使用量と、サイズ上限・失敗時の挙動を確認する。
- [ ] 低解像度プレビューを使う場合は、最終出力との違いを比較して採否を相談する。
- [ ] ドラッグ中の処理品質、再計算のタイミング、解像度制約を相談して決める。

ここまでが初期プロトタイプの評価範囲。画質と速度の両方を見て、次の段階へ進むか判断する。

## 6. 評価後：複数ストロークと制作向け機能

- [ ] 複数線の追加・削除、線選択パネル、選択中の強調、表示切り替え、順序変更を作る。
- [ ] 複数線の重なり順と選択UIを決める（新Bの採取元は元画像）。
- [ ] 必要な再計算範囲と中間画像の保存方法を設計し、複数線の編集性能を検証する。
- [ ] 元画像と編集情報をまとめて保存・再読込できるようにする。
- [ ] 印刷向けの出力解像度・色・階調・ファイル形式を確定して実装する。
- [ ] 必要に応じて余白や切り抜き、最終調整の機能を検討する。

## 次に確認すること

Prototype 01の起動方法は[README](README.md)、検証済みの操作と性能値は[実装メモ](IMPLEMENTATION.md)を参照。

次は実際の写真でA/Bの表現、採取線の操作、太い曲線の折り返し、5000px時の編集体感を一緒に評価する。実装済みでも美的な評価や印刷品質の検証は完了扱いにしない。低解像度専用プレビューは導入していない。
