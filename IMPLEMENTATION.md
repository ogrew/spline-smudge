# Prototype 01 — 実装・検証メモ

2026-09-10

## 構成

Vite + TypeScript、DOM/SVGの編集ガイド、WebGL2の作品描画。p5.jsに依存しない。

点・基本幅・採取範囲は元画像座標で保持し、出力時に同比率で拡大縮小する。初期出力は長辺2000px。5000pxを選んだときも5000pxの処理結果を表示し、低品質の別プレビューは使わない。

JPEG/PNGの署名を確認し、`createImageBitmap` の `from-image` でデコード。sRGBの2Dキャンバスで選択解像度への変換と背景合成を行う。これは読み込み／出力サイズ／背景変更時のみ。元のFileとImageBitmapは保持する。ストローク処理中はCPUで写真の全画素を操作しない。

### A

曲線を弧長で再サンプリングし、接線に垂直な方向へ幅を持たせる。断面の色参照先は独立した採取線上の位置。採取範囲が写真外に出る場合は端の色を延長（clamp）する。後の区間が前の区間に重なる。

### B

採取線から色の断面を作り、進行方向に合わせて回転する。次の位置で写真／同じ線ですでに描いた部分の色を拾い、保持した断面と混ぜ、区間を描画する。

- 色保持用の1行テクスチャ2枚を交互に使う。中間色保持はRGBA16F。小さな拾い量が8bit丸めで消えるのを避ける。`EXT_color_buffer_float` 未対応ではBを明示的に停止する。
- 写真・作業・完了結果は別々のRGBA8ターゲット。同じ画像を読みながら書き込まない。
- 混合率は `1 - exp(-pickup * distance / (baseWidth * 0.4))`。distanceは元写真座標の移動距離で、イベント頻度・画面ズームに依存しない。
- 拾う量や距離が大きいと筋のコントラストが薄まる。現在の表現特性であり、筋の保持・混色の両立はユーザーと評価する。
- 自己交差では同じ線で先に描いた領域の色も拾う。複数線の仕様は今回の対象外。

### 編集・出力

編集要求ごとに世代番号を更新し、古い処理を中断。Bのコマンド投入は約8msごとにブラウザへ制御を返す。毎回元写真から再計算し、完了した世代だけを表示結果と交換する。計算中は前の完了画像に新しいガイドが重なるが、書き出しはできない。

GPU完了待ちとPNG読み戻しにfence／pixel pack bufferを使う。画素読み戻しは出力時だけ。ガイドは別SVGでPNGに入らない。背景合成後の出力alphaは1を保つ。

5000 × 3438pxでは写真・作業・完了の3枚だけで約206MB（十進表記）。表示バッファ、元画像、PNG出力時のCPUコピーが別途必要。GPU上限チェックはメモリ確保の保証ではないため、確保失敗も扱う。

## 検証結果

macOS、Node 24.4.1、ローカルChrome。生成パターン、色領域を分けた透明PNG、EXIFの向き情報を埋めたJPEGを使用。ユーザーの写真をテストやGitに含めていない。

- 数学・状態のテスト10件：5種類の端点・有限値・幅の範囲、TCBのゼロ設定、重複点、ゼロ幅、履歴、出力比率。
- 型検査と本番ビルド成功。
- 実ブラウザ：5種類の描画、A/B切替の点保持、0/10倍、ドラッグ、ダブルクリック削除と取り消し、番号、採取線移動、中断、連続編集の最新値、A/Bの5000px PNGを確認。
- 透明PNGの4箇所の色とalphaを画素比較。JPEGのEXIF回転、不正形式の拒否、GPUコンテキスト復旧を確認。
- 5000 × 3438pxの一例：A描画32ms、B描画62ms。PNG出力A236ms、B274ms。GPU完了待ちを含む単発測定。デモ元画像は1600 × 1100px。写真一般の性能保証ではない。
- 書き出したA/Bを開き、向き・筋・Bの混色・ガイドが入らないことを確認。
- A/Bを往復して再計算したAのPNGは元のAと完全一致。ガイドと番号の表示を切り替えてもPNGは完全一致。100%表示の確認画像も取得した。

ブラウザの手順は `tests/browser.spec.ts` に記録。Google Chromeと起動済み開発サーバーを使う。検証画像は `/private/tmp/spline-smudge-*.png`。写真・テスト出力・node_modulesはGitに含めない。

## 続けて相談すること

実際の写真でのBの混ざり方、急な曲がり・交差・端の見た目、採取線の使いやすさ、5000px時の編集体感を優先する。印刷での細部・階調・色の評価はまだ。複数線、作品保存、広色域／16bit、印刷用余白は後続段階。

## 確認した資料

- [MDN: WebGL best practices](https://developer.mozilla.org/en-US/docs/Web/API/WebGL_API/WebGL_best_practices)：資源管理、非同期読み戻し、端末ごとの上限。
- [MDN: createImageBitmap](https://developer.mozilla.org/en-US/docs/Web/API/Window/createImageBitmap)：画像の向きと色変換オプション。
