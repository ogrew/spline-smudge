# Prototype 01 — 現在の実装・検証メモ

更新：2026-09-11、複数ストロークを実装。Bは各点の色補間方式。

## 複数ストローク

スプライン種類はDocumentState.kindに一度だけ保持し、各線には保持しない。描画と選択線のガイド計算に共通のkindを渡す。選択UIは線一覧の上へ配置。既存・非表示・新規・複製のすべてが同じ種類で、変更は作品履歴の取り消し対象。太さ・採取線・TCB調整値は線ごとに保持する。エクスポート名も作品全体のkindから決める。

Strokeに安定したID・名前・visibleを保持する。配列順は奥から手前、UIは逆順。選択変更は描画内容を変えず、ガイド・パラメータ表示だけを更新する。複製は深いコピーと新しい線・点IDを作り、同じ位置の最前面に追加する。新規線は点を空にして、選択中の設定を初期値として使う。最後の線を削除したときは空の線を残し、常に有効なactiveIdを保つ。

描画では写真を作業ターゲットへ1回コピーし、表示中の全線を順に合成する。各線の採取元は元写真。作業・完了ターゲットを線ごとに増やさず、メッシュ用GPUバッファも使い回す。線の処理間で経過時間を確認してブラウザへ制御を返し、古い世代は破棄する。線数が増えると再描画量と履歴データは増えるが、写真サイズの中間テクスチャを線数分保持しない。

検証：数学・状態テスト16件、実ブラウザ4シナリオ成功。複製の独立性、選択、追加・削除、非表示、順序、履歴、最後の削除、複数種類を含む5000px PNGを確認。A/Bとも赤と青の交差線で、順序変更と非表示がエクスポート画素へ正しく反映されることを確認した。画面の線一覧と選択ガイドも画像で確認。

## 構成

Vite + TypeScript、DOM/SVGの編集ガイド、WebGL2の作品描画。p5.jsに依存しない。

点・基本幅・採取範囲は元画像座標で保持し、出力時に同比率で拡大縮小する。長辺5000pxを選んだときも5000pxの処理結果を表示し、低品質の別プレビューは使わない。

JPEG/PNGの署名を確認し、`createImageBitmap` の `from-image` でデコード。sRGBの2Dキャンバスで選択解像度への変換と背景合成を行う。元のFileとImageBitmapは保持し、ストローク処理中にCPUで写真の全画素を操作しない。

## Aと新B

Aは始点を中心とする1本の採取線を帯全体に使う。Bは各制御点に角度・長さを保持し、元写真のその位置で採取する。採取線の中心は点から独立して移動しない。B-splineでも採取元はクリックした制御点で、曲線上のGreville対応位置にその色を割り当てる。

両モードで同じ帯状メッシュを使う。Bでは各頂点に隣接2点の採取座標と区間進行度を渡し、fragment shaderで元画像を2回参照して補間する。各点の角度・長さが違っていても、正規化した帯幅の左端同士・中央同士・右端同士が対応する。色の筋を横に移動して対応付ける処理ではない。

曲線のサンプルが色の区間境界をまたぐ場合はメッシュを分割し、隣接区間の接合部で同じ採取色を使う。連続する同位置の点は最後の点を代表とし、幅とBの採取設定を使う。写真外の採取は端の色を延長（clamp）する。帯の自己交差は描画順に重なるが、描いた色を再採取しない。

## 補間方式の変更箇所

`src/color-interpolation.ts` にGLSLの2関数を分離。

- `colorWeight(t)`：区間の補間量。現在は `smoothstep(0,1,t)` で、点の前後の変化を穏やかにする。
- `interpolateColor(a,b,t)`：色の混ぜ方。現在は符号化されたsRGBのRGB成分を直接補間。linear-light RGB、HSV等への変更はこの関数に変換・逆変換を追加する。新しい方式は画素テストも更新する。

現時点で色空間の選択UIはない。出力はsRGB・8bit PNG。HSVの色相の回り方や無彩色の扱い、CMYKのプロファイル等は、その方式を追加する時点で決める。

## 状態・描画・出力

Aの採取設定はStroke、Bの採取設定は各Pointに保持する。新しい点は作成時のA設定から角度・長さをコピーし、以後独立。A/B切替で固有設定を失わず、取り消し・やり直しにも含まれる。

写真・作業・完了結果は別々のRGBA8ターゲット。元画像だけを参照するので、旧Bの逐次混色・RGBA16F色保持・浮動小数点描画拡張は不要になった。編集要求は世代番号で管理し、GPU完了時に最新の世代だけを表示結果と交換する。未完成画像は出力できない。

GPU完了待ちとPNG読み戻しにfence／pixel pack bufferを使う。画素読み戻しはエクスポート時だけ。ガイドは別SVGでPNGには入らない。背景合成後の出力alphaは1。

5000 × 3438pxでは写真・作業・完了の3枚だけで約206MB（十進表記）。表示バッファ、元画像、メッシュ、PNG出力時のCPUコピーが別途必要。サイズ上限と確保失敗を検出し、無断で解像度を下げない。

## 検証

macOS、Node 24.4.1、ローカルChrome。生成パターン、透明PNG、EXIFの向きを埋めたJPEGを使用。ユーザーの写真はテストやGitに含めない。

- 数学・メッシュ・状態テスト13件成功。5曲線の端点・幅・重複点、TCB、履歴、比率に加え、B-splineの採取元が制御点であること、色の区間境界、AがB設定に影響されないことを確認。
- 型検査・本番ビルド成功。
- 実ブラウザの2シナリオ成功。点ごとの角度・長さと履歴、A/B設定の保持、選択・移動・削除、4〜12点のランダム生成、中断・連続編集、5000px PNG、JPEGの向き、透明PNGの色、GPU復旧。
- GPU画素比較：赤→緑→青の採取線で、各点の色と中間色が期待RGB値になることを確認（画素中心位置による差に許容4/255）。点間の背景を黄色へ変更しても、Bの中間色は変化しない。
- A/B往復後のAのPNGは元のAと完全一致。番号・ガイドの切替でもPNGは完全一致。
- 5000 × 3438pxの今回の一例：新B描画44ms、A描画31ms。PNG出力B290ms、A236ms。GPU完了待ちを含む単発測定であり性能保証ではない。元の生成パターンは1600 × 1100px。
- 新Bの画面を画像で確認。各点の採取線と選択表示、区間ごとの色の変化が出ている。

検証画像は `/private/tmp/spline-smudge-*.png`。テスト手順は `tests/browser.spec.ts`。旧BはGit履歴に残し、現在のUIには出さない。

## 次の評価

実際の写真でRGBクロスフェードの見た目、複数線の操作感、太い帯の折り返し・交差を評価する。印刷での細部・階調・色、作品保存、広色域／16bitは後続段階。

## 確認した資料

- [MDN: WebGL best practices](https://developer.mozilla.org/en-US/docs/Web/API/WebGL_API/WebGL_best_practices)：資源管理、非同期読み戻し、端末ごとの上限。
- [MDN: createImageBitmap](https://developer.mozilla.org/en-US/docs/Web/API/Window/createImageBitmap)：画像の向きと色変換オプション。
