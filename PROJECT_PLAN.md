# Spline Smudge — プロジェクト計画

作成日：2026-09-10  
状態：Prototype 01実装済み。以下は計画時の議論を残したものです。現在の操作・制約は[README](README.md)、検証結果は[実装メモ](IMPLEMENTATION.md)を参照してください。

## 1. 何を作るか

**スプライン種類の共通化（2026-09-11）**：種類を作品全体で1つ選ぶ仕様へ変更。各線の種類選択は廃止し、B-splineなら全線B-spline、Natural cubicなら全線Natural cubicとする。以下の各線に種類を保持する記述は以前の仕様。

**複数線の追加合意（2026-09-11）**：線一覧、追加・複製・削除、表示／非表示、重なり順の変更を実装。新しい線を最前面に追加し、一覧上側を手前とする。選択中の線だけにガイドを表示。A/Bは作品全体に適用し、各線の形・太さ・採取設定は独立して保持する。エクスポートは表示中の全線を含める。

**現在のB仕様（2026-09-10の追加合意）**：計画中の「色を拾いながら引きずるB」は最初の試作時の案。評価後、各点に採取線を持たせ、その間を補間する方式へ置き換えた。各点を選んで角度・長さを編集する。B-splineでも採取位置はクリックした制御点で、採取元は元写真。RGB補間で開始し、別方式へ差し替えやすい構造とする。AとBの採取設定は独立して保持する。以下の旧案は議論の履歴として残す。

自分で撮影した写真を読み込み、その色情報をスプラインに沿って引き伸ばして、写真とグラフィックが混ざる画像作品を作るブラウザツール。

単色の線を写真の上に描くのではなく、写真に含まれる複数の色や細かな模様を線に沿って運ぶ。小林健太の写真を加工したストローク表現を参考にしているが、特定作品やPhotoshopの指先ツールとの完全な一致を目標にするものではない。

操作の中心は、クリックで点を置き、点の位置と太さをあとから編集すること。フリーハンド描画よりも、形を見ながら調整できるスプライン編集を優先する。

最終用途は写真作品の印刷。A4〜A3を想定し、A2は現時点の対象としない。最大サイズはまだ確定していない。

## 2. 合意した方針

| 項目 | 合意内容 |
| --- | --- |
| 独立プロジェクト | 既存のp5-drawingとは別フォルダで新規開発する |
| 技術構成 | TypeScript／JavaScript＋WebGL2＋GLSL。p5.jsに依存しない |
| 入力画像 | JPEG／PNG。TIFFは初期対象外 |
| 写真の種類 | フィルム写真のスキャン、デジカメの写真の両方。解像度・縦横比は不定 |
| 初期プロトタイプ | 写真1枚・編集可能なスプライン1本でA/Bを切り替える |
| モードの単位 | ツール全体でAまたはBを選ぶ。同じ作品内でストロークごとにA/Bを混在させない |
| スプライン | 現在の5種類を引き継ぐ |
| 線幅 | 基本太さpx × 点ごとの倍率0〜10倍。前後の点へ滑らかにつなぐ |
| 編集 | 点の追加・移動・削除、太さの調整、取り消し |
| 将来の複数線 | 複数ストロークと、編集対象が分かる線選択パネルを導入する |
| 開発の進め方 | 表現・性能の不確かな部分は試作で比較し、重要な妥協はユーザーと相談して決める |

Bは現時点で断念しない。まず1本で見た目と負荷を確認し、その結果から複数線への展開を判断する。

## 3. A/Bの表現

### A：採取した色の帯を伸ばす

写真の一部分から、複数の色が並んだ断面・細い領域を採取し、スプラインに沿って伸ばす。平均色や単色を取る方式ではなく、色の並びを残すことが重要。

想定する特徴：

- 色の筋が続く、リボン状の表現。
- 元写真を参照して再描画できるため、点編集・取り消しとの相性がよい。
- 曲線が通った場所の色を逐次混ぜる動作は、この方式の基本には含めない。

採取位置・採取幅・採取方向をどう操作するかは未確定。採取範囲と描画幅を分ける案を検証する。

### B：色を拾いながら引きずる

始点付近の色を運びながら、通過する位置の色を取り込み、写真をこすったようなストロークを作る。

想定する特徴：

- 色の筋が途中で変わったり、混ざったりする。
- ストロークの進行方向と処理順序が見た目に影響する。
- 1本の曲線を変更したときも、そのストロークを元写真から再計算する必要がある。

ブラシが保持する色情報、色を拾う量、混合率、輪郭の硬さ、回転の扱いなどは検証対象。Photoshop内部のアルゴリズムが判明したという前提では進めない。

### 最初に比較すること

同じ写真・同じ点配置・同じ幅設定でA/Bを切り替え、以下を確認する。

- 色の筋が残るか。単なるぼかしや単色の線になっていないか。
- 写真のどこから色を取ったか分かるか。
- 強い曲がり、折り返し、太さの変化が魅力的か。
- 編集のしやすさと処理時間が、表現の面白さに見合うか。

## 4. 引き継ぐスプラインと操作

1. Uniform Catmull–Rom
2. Bスプライン（開いた曲線・クランプされた端点）
3. Centripetal Catmull–Rom
4. 自然3次スプライン
5. Kochanek–Bartels／TCB（張り・折れ具合・偏り）

クリックで点を追加し、ドラッグで移動する。点のダブルクリック削除、点番号・補助表示の切り替えも既存の操作として引き継ぐ。

基本線幅は画像／出力座標上のpxとし、各点の倍率は0〜10倍。幅の補間では不意に負の値や過大な値にならないようにする。Bスプラインは内部の制御点を通常通らないため、対応する曲線上の位置で幅を調整する意味づけを引き継ぐ。

既存の数学的な計算・操作仕様を再利用する。単色の線をCanvas 2Dで塗る既存の描画処理を、そのまま画像加工に流用するわけではない。

## 5. 初期プロトタイプの利用の流れ

1. JPEGまたはPNGを1枚読み込む。
2. 元の縦横比を維持して写真を表示する。
3. 写真上に点を置き、1本のスプラインを作る。
4. スプラインの種類、点位置、基本線幅、各点の倍率を調整する。
5. 写真の色を採取する位置・範囲を設定する。操作方法は比較試作時に検証する。
6. A/Bを切り替えて結果を見る。切り替え時に元写真や点配置を失わない。
7. 100％表示や拡大表示で筋や輪郭の品質を確認する。
8. 検証用画像を出力し、長辺5000px程度で結果を確認する。

初期プロトタイプで複数線のUIを完成させることは求めない。ただし、内部では最初からストローク一覧として扱い、後から複数本を追加できるようにする。

## 6. 技術構成案

| 層 | 役割 |
| --- | --- |
| UI・編集 | TypeScript／JavaScriptで入力、点・線の選択、設定、履歴を管理 |
| 曲線計算 | スプライン、幅、進行方向、ブラシを進める距離を計算 |
| GPU描画 | WebGL2とGLSLで画像の参照、引き伸ばし、混合、合成を行う |
| 編集用表示 | 点、番号、補助線、採取範囲を作品と分けて表示 |
| 出力 | 元画像と編集情報から指定解像度で再計算し、画像を書き出す |

Aでは、スプラインに沿う帯状の面へ元写真の色を割り当てる方式を候補とする。Bでは、ブラシを順番に進め、読み取り元と書き込み先の画像をGPU上で分けて処理する方式を候補とする。

CPUとGPUの間で毎回画像全体を往復させる設計は避ける。GPUでもBの順次処理は残るため、ブラシが触る範囲の更新、再計算の中断、古い更新要求の破棄などを検討する。

具体的なUIライブラリ、ビルドツール、GPUの中間画像形式はまだ選定していない。

## 7. 複数ストロークへの拡張

ストロークごとにID、順序、点、倍率、曲線の種類、必要な採取・ブラシ設定を保持する。A/Bのモードは作品全体の設定とする。

将来の線選択パネルには、一覧、選択中の強調、追加・削除、表示切り替え、重なり順の操作を想定する。細かなUIは複数線を実装する段階で相談する。

Bの複数線には、次の未確定事項がある。

- 後の線が元写真だけを読むのか、前の線で加工された画像も読むのか。
- 過去の線を変更したとき、後続の線をどこまで再計算するのか。
- 中間結果をどれだけ保持し、速度とメモリ消費をどう両立するか。

初期構造では再編集できる余地を残すが、多数のBストロークを常に即座に編集できるとは約束しない。

## 8. 印刷・画像品質

用途はA4〜A3。300ppiでの概算はA4が2480×3508px、A3が3508×4961pxなので、長辺5000px程度を検証対象とする。これは保証済みの性能上限や必須入力サイズではない。

元写真は保持し、表示用の縮小画像で置き換えない。写真の縦横比を尊重し、A判への自動切り抜きは行わない方針。出力は写真の比率と長辺の画素数を基準にする案を採用候補とする。紙に合わせた余白・切り抜きUIは後の検討事項。

A/Bとも100％表示で確認し、Bは特に繰り返し処理によるぼけ・筋の消失・階調の段差を検証する。

以下はまだ確定していない。

- JPEG／PNGのICCプロファイル、色空間、PNGの階調、透過の扱い。
- GPU処理と最終出力を8bit／16bitのどこまで対応させるか。
- 印刷用の出力形式。検証用PNGと最終的な制作出力の仕様は分けて判断する。
- 簡易プレビューと高解像度再計算の見た目の差をどこまで許容するか。

TIFFの入力対応は初期対象外。RAWの直接読み込みも初期対象に含めない。

## 9. 判断が必要な事項とタイミング

| 判断事項 | 判断する段階 |
| --- | --- |
| 採取範囲と描画幅の分離、採取位置・方向の操作 | A/Bの小さな比較試作を作る前〜比較時 |
| Bで色を拾う量、ブラシの輪郭・回転・幅変更時の色の扱い | 1本のBを見ながら |
| A/B切り替え時の採取設定の共有・保持 | 1本の比較UIを設計するとき |
| 読み込み時の色・階調・透過と、初期出力の制約 | 画像読み込みを実装する前 |
| ドラッグ中の更新か、マウスを離してから再計算するか | 実機で処理時間を測ったあと |
| 低解像度プレビューの採用 | 高解像度結果との比較後 |
| Bの複数線で、前の加工結果も拾うか | 1本のBの評価後 |
| 印刷向け最終形式・色管理・対応解像度 | 画質検証後、制作版に進む前 |

これらの重要な選択や品質・操作上の妥協は、ユーザーへの相談なしに固定しない。計画書中の「案」「候補」は合意済みの仕様と区別する。

## 10. 最初の到達点

「同じ写真・同じスプラインを使ってA/Bを比較でき、見た目、操作感、高解像度画質、処理負荷を判断できる状態」。

この段階でBを断念するか、制約付きで残すか、複数線へ進めるかを相談する。見た目が意図と違えば、編集UIを増やす前に画像加工方式を見直す。

## 参考資料

### 実装前に追加で合意した内容

- 初期処理・出力はsRGB・8bit PNG。透明PNGは選択した背景色に合成し、元画像を保持する。広色域・16bitの制作版は後続検討。
- 採取線は写真上で位置・角度・長さを編集できる。描画幅とは独立させる。
- Bも同じ採取線から始め、進行方向へ回転して色を拾う。点を変えたら元写真から再計算する。

Prototype 01は元写真座標を共通基準とし、出力時に作品全体を比例させる。RGBA16Fのブラシ保持は丸め誤差対策の内部形式であり、16bit出力対応を意味しない。低解像度専用プレビューは未導入。

### 資料

制作手法は、この会話で確認した以下の資料を背景とする。作品ごとの設定値や実装の詳細が判明したことを意味しない。

- [小林健太の公式ポートフォリオ](https://www.kentacobayashi.com/portfolio-foam)
- [TOKION：小林健太のインタビュー](https://tokion.jp/2021/04/13/kenta-cobayashi-genre-defying-photography/)
- [GIZMODO：指先ツールと制作機材について](https://www.gizmodo.jp/article/raytrek-zg-cobayashi-kenta/)
- [Adobe：Smudge Tool](https://helpx.adobe.com/uk/photoshop/using/tool-techniques/smudge-tool.html)
- [MDN：WebGLのベストプラクティス](https://developer.mozilla.org/en-US/docs/Web/API/WebGL_API/WebGL_best_practices)
